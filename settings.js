var settings = {
  root: "C:/Users/Delegado",
  entryType: "all",
  // Filtrar archivos con extensión pdf y doc
  fileFilter: ["*.pdf", "*.doc"],
  // Filtrar por directorio
  // directoryFilter: ["!.git", "!*modules"],
  // Trabajar con archivos de hasta 1 subdirectorio de profundidad
  // depth: 1,
};
